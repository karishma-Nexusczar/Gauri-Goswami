import { Metadata } from "next";
import LegalCredentialsSection from "../../components/LegalCredentialsSection";

export const metadata: Metadata = {
  title: "Legal Credentials | Gauri Goswami",
  description: "Verified professional credentials and registration as an Advocate.",
};

export default function CredentialsAliasPage() {
  return <LegalCredentialsSection />;
}

